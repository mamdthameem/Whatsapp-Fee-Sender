{
  "name": "wa-pdf-webpage",
  "private": true,
  "description": "WhatsApp Fee Sender - Firebase Hosting + Functions",
  "scripts": {
    "postinstall": "npm run install:all",
    "install:functions": "npm install --prefix functions",
    "install:backend": "npm install --prefix backend",
    "install:all": "npm run install:functions && npm run install:backend",
    "deploy": "firebase deploy"
  }
}
